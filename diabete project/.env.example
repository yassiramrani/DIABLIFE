# API base URLs — copy to .env and set for your environment.
# .env is gitignored; never commit real secrets.

# Emergency-call server (Node.js, default port 3001)
VITE_API_URL=http://localhost:3001

# Auth / backend API (e.g. Python FastAPI, default port 8000)
VITE_AUTH_API_URL=http://localhost:8000
